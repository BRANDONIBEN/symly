# macOS
.DS_Store

# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# Xcode
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint

# Build / release artifacts
dist/
*.dmg
*.app

# Local secrets (notarization creds, etc.) — never commit
.env
*.p12
*.cer
notary-*.json
